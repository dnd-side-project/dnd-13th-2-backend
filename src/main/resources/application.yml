spring:
  application:
    name: eodigo
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    open-in-view: false
